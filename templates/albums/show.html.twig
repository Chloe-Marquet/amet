{% extends 'base.html.twig' %}

{% block title %}Albums{% endblock %}

{% block body %}
    <h1>Albums</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ album.id }}</td>
        </tr>
        <tr>
            <th>Title</th>
            <td>{{ album.title }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('albums_index') }}">back to list</a>

    <a href="{{ path('albums_edit', {'id': album.id}) }}">edit</a>

    {{ include('albums/_delete_form.html.twig') }}
{% endblock %}
